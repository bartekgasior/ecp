import styled from "styled-components";

const DefaultButtonContainer = styled.img`
    height: ${({ height }) => `${height}px`};
    width: ${({ width }) => `${width}px`};
`;

export default DefaultButtonContainer;