import styled from "styled-components";

const SimpleWarningContainer = styled.span`
    font-size: 0.75em;
    color: var(--error);
    font-weight: bold;
    padding: 0.25rem 2rem;
    display: flex;
    justify-content: center;
`

export default SimpleWarningContainer;